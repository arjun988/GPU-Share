# GPUMesh Cloud Dashboard (Phase 6)

```bash
# API
cd ../services/control-plane && go run .

# UI
npm install
npm run dev
```

Open http://127.0.0.1:3000

Optional: `NEXT_PUBLIC_GPUMESH_API=http://127.0.0.1:8080`

Sync node data: `gpumesh sync`
